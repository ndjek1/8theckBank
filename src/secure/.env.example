# Copy to .env and fill in. Never commit secrets to git.
# Generate strong values with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=replace-me-with-strong-random-value
JWT_SECRET=replace-me-with-strong-random-value
